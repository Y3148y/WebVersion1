# 前后端联调启动指南

## 1. 后端服务启动步骤

### 步骤1：创建MySQL数据库

1. 打开MySQL客户端（如MySQL Workbench或命令行）
2. 执行以下SQL命令创建数据库：
   ```sql
   CREATE DATABASE demo_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

### 步骤2：配置数据库连接

1. 打开`demo/src/main/resources/application.properties`文件
2. 修改数据库连接配置：
   ```properties
   spring.datasource.username=root
   spring.datasource.password=your_password
   ```
   将`your_password`替换为你的MySQL密码

### 步骤3：启动后端服务

#### 方式1：使用IntelliJ IDEA启动

1. 打开IntelliJ IDEA
2. 选择`File` -> `Open`，选择`demo`目录
3. 等待IDEA加载项目和依赖
4. 找到`DemoApplication.java`文件（位于`src/main/java/com/example/demo/`目录下）
5. 右键点击该文件，选择`Run DemoApplication`
6. 等待服务启动成功，控制台输出"Started DemoApplication in xxx seconds"

#### 方式2：使用Maven命令启动

1. 打开命令行终端
2. 进入`demo`目录
3. 执行以下命令：
   ```bash
   mvn spring-boot:run
   ```
4. 等待服务启动成功

## 2. 前端页面访问

1. 打开浏览器
2. 访问`index.html`文件（可以直接双击文件或使用本地服务器）
3. 打开浏览器控制台（按F12）
4. 查看控制台输出：
   - 如果显示"✅ 后端API服务正常运行"，说明前后端连接成功
   - 如果显示"⚠️ 后端API服务不可用"，请检查后端服务是否启动

## 3. 功能测试

### 测试1：加入我们功能

1. 点击页面底部的"立即加入"按钮
2. 填写表单信息，点击"提交"按钮
3. 如果显示"🎉 加入成功！欢迎成为我们的一员！"，说明用户创建成功
4. 查看控制台输出，应该显示"📝 成功创建用户:"和用户信息

### 测试2：API健康检查

1. 访问`http://localhost:8080/api/users/health`
2. 如果返回"OK"，说明API服务正常

### 测试3：获取用户列表

1. 访问`http://localhost:8080/api/users`
2. 应该返回用户列表，初始状态为空数组`[]`，创建用户后会显示用户信息

## 4. API文档访问

1. 启动后端服务后，访问`http://localhost:8080/swagger-ui.html`
2. 可以查看所有API接口的详细信息
3. 可以在页面上直接测试API接口

## 5. 项目结构说明

### 后端项目结构

```
demo/
├── src/
│   ├── main/
│   │   ├── java/com/example/demo/
│   │   │   ├── config/           # 配置类
│   │   │   │   └── OpenApiConfig.java  # OpenAPI配置
│   │   │   ├── controller/       # 控制器层
│   │   │   │   └── UserController.java  # 用户API控制器
│   │   │   ├── entity/           # 实体类
│   │   │   │   └── User.java     # 用户实体
│   │   │   ├── repository/       # 数据访问层
│   │   │   │   └── UserRepository.java  # 用户Repository
│   │   │   ├── service/          # 业务逻辑层
│   │   │   │   ├── UserService.java      # 用户Service接口
│   │   │   │   └── impl/
│   │   │   │       └── UserServiceImpl.java  # 用户Service实现
│   │   │   └── DemoApplication.java  # 主启动类
│   │   └── resources/
│   │       └── application.properties  # 配置文件
│   └── test/                     # 测试目录
└── pom.xml                       # Maven依赖配置
```

### 前端项目结构

```
.
├── index.html          # 首页
├── script.js           # JavaScript代码
├── styles.css          # CSS样式
├── images/             # 图片资源
└── 其他页面文件...
```

## 6. 常见问题解决

### 问题1：后端服务启动失败

- 检查MySQL服务是否启动
- 检查数据库连接配置是否正确
- 检查端口是否被占用（默认端口8080）

### 问题2：前后端连接失败

- 检查后端服务是否正常运行
- 检查浏览器控制台是否有跨域错误
- 检查API_BASE_URL配置是否正确

### 问题3：数据库表创建失败

- 检查MySQL驱动版本是否兼容
- 检查实体类注解是否正确
- 检查数据库权限是否足够

## 7. 扩展建议

1. 添加用户认证和授权功能
2. 扩展更多业务模块
3. 添加数据校验
4. 实现分页和排序
5. 添加日志记录
6. 实现文件上传功能
7. 添加WebSocket实时通信

通过以上步骤，你可以成功启动前后端服务并进行联调测试。如果遇到问题，请查看控制台输出和日志信息，根据错误提示进行排查。